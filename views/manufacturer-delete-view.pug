extends layout 

block content 
  h2= manufacturer.name 

  if manufacturerProducts.length 
    p Delete the following products before attempting to delete this manufacturer

    ul
      each product in manufacturerProducts 
        li 
          a(href= product.url) #{product.name} 
  else 
    p Do you really want to delete this manufacturer

    form(method="post") 
      input#manufacturer-id(type="hidden", name="manufacturer-id", value= manufacturer._id) 
      button(type="submit") Delete